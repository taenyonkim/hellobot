# 개발 상태

## 현재 상태: 운영 배포 완료 (Phase 1 + 핫픽스 2건, 후속 안정화·QA·데이터 파이프라인 진행)

> 2026-04-30: **운영 배포 + 핫픽스 2건 (ISS-053, ISS-054)**. ① ISS-053 — `coop_marketing_api_log.result_code` varchar(4) 위반(catch 블록이 "TIMEOUT" 7자 INSERT 시도 → 22001 SQL 에러가 실제 `CM_NETWORK_ERROR` 마스킹). 핫픽스 PR #2401 (commit `f2f12a84`) — catch 3곳 `saveApiLog` 제거 + `winston.error` 마스킹 로그. master 머지 + 운영 ArgoCD sync + deploy-dev cherry-pick(`c5245d05`) 완료. ② ISS-054 — 운영 URL 스킴 오류(`http://authapi.inumber.co.kr:443` → 즉시 RST). 핫픽스 PR #2403 (commit `8372dadc`) — `src/common/config.ts:591` `https://authapi.inumber.co.kr/AuthUse` 로 수정. master 머지 + 운영 ArgoCD sync 완료. 사용자 curl 검증으로 통신 정상(TLS 1.2 + HTTP/2 200 + 정상 ResultCode) 확인. ③ ISS-052 등록 (L1+L3 동시 실패 시 (나) 시나리오 자동 복구 불가 — CS 유의미 발생 시 구현, 현재는 winston 로그 기반 CS 수동 처리).

> 2026-04-19: ISS-011 + ISS-009 해결을 위한 **아키텍처 전면 개편**. 서버 단일 진입점 + 1단계 원샷 플로우로 전환.
> 2026-04-20: 서버/iOS/Android/웹 Phase 1 구현 완료. QA TC 재편성 완료 (145건, xlsx v5). 배포 및 QA 검증 단계 진입.
> 2026-04-21: ISS-021/022 (expiresAt 무제한 + sentinel + `isUnlimited` 재설계) 서버·iOS·Android·Web 해결 완료. ISS-023/024 (S4 링크 우측 정렬) iOS·Android 해결. ISS-016 Web 해결 (토스트 2.5초). ISS-017 Web 해결 (쿠폰 리스트 즉시 반영). "모바일 웹뷰 환경 검증" 항목은 아키텍처 확정(앱 네이티브 / 웹 브라우저 독립) 결과 해당 없음으로 종결.
> 2026-04-21: Notion 스프린트 태스크 5건을 ISS-025~029로 등록. ISS-025 웹 재진입 시 스킬 카드 하단 유실(P2), ISS-026 iOS 쿠폰 에러 메시지 표시 오류(P2), ISS-027 iOS 스킬 보러가기 화살표 이미지 리소스 교체(P3), ISS-028 Android S3 모달 여백·이미지 크기(P2), ISS-029 AdminJS 메뉴명 한글화(P3). 각 파트 tasks.md에 과업 추가.
> 2026-04-21: Android ISS-016/019/020/028 일괄 해결 재확인 및 반영. ISS-028(S3 모달) — design-spec "공통 컴포넌트 팝업"(288dp / shadow `0 8 24 rgba(0,0,0,0.24)`) 규칙에 맞춰 카드 폭 288dp 고정 + shadow(elevation 24dp) + 일러스트 240×117dp로 수정.
> 2026-04-21: iOS ISS-026/027 해결. ISS-026은 `CouponRegisterErrorMapper` 신설로 서버 ko 메시지 우선/폴백 + 오프라인 한글 메시지 치환. ISS-027은 "스킬 보러가기" 라벨 + SF Symbol `chevron.right` (violet400 틴트) horizontal stack으로 텍스트 `>` 제거.
> 2026-04-22: Android ISS-027 해결 — `coop_link_view_skill` 문자열 3종(ko/ja/en)에서 ` >` 제거 + `CouponItem.kt` 우측 링크를 `Row(Text + Icon)` 구조로 재구성. 기존 벡터 드로어블 `icon_arrow_right_16` 재사용 + `Icon(tint = violet400)`로 색 오버라이드.
> 2026-04-22: Web QA 이슈 4건 해결 — ISS-032(화살표 violet inline SVG), ISS-037(라벨 스타일 CouponItem 일치), ISS-042(버튼 인라인 스피너 + 풀스크린 Loading 제거), ISS-036(bfcache 복원 시 `pageshow` 훅 + `mutate('/api/coupon')` 재조회). ISS-031/034/035/033은 외부 의존(디자이너/서버/Android/아키텍트) 답변 대기.
> 2026-04-22: Web QA 이슈 2건 추가 해결 — ISS-031(사용자 결정 준용: `CoopSkillVoucherItem`을 `CouponItem` 수직 리듬 모방 + 서브텍스트 `invisible` reserve로 카드 높이 동일화), ISS-035(사용자 결정 준용: 신규 자산 발급 없이 기존 `public/images/heart/heart_charge.gif` 재사용, `BonusHeartModal`의 plain `<img>` + `?t=` 캐시 버스터 패턴 이식). 잔여: ISS-033(`/architect` 대기), ISS-034(QA 재검증 대기).
> 2026-04-22: iOS ISS-042 해결 — 사용자 결정(스피너 미구현, disable + 회색만) 반영. `CouponInputFieldView`에 `isInputFilledRelay` + `isRegisteringRelay` BehaviorRelay 2개 + `setupContext()` combineLatest(`filled && !registering`) → `sendButton.rx.isEnabled` 단일 진실 공급원. `CouponListViewController.registerCoupon(code:)` `.do(onSubscribe:/onDispose:)` 훅으로 API 라이프사이클 전체 리셋 보장. 회색은 기존 disabled 토큰(gray400/gray200) 재사용, 신규 색상 정의 없음.
> 2026-04-23: iOS ISS-039 해결 — 서버 i18n 배포(ISS-044 포함, 커밋 ba674b3b) 후 `CouponRegisterErrorMapper` 판정 완화. `resolve()` / `ReasonServerError` 경로에서 `containsHangul` 게이트 제거 → 서버 `message`/`reason` non-empty 시 그대로 표시(ja/en 번역 자연 반영), 빈 문자열·offline 등 서버 응답 없음 경로만 `codeMessages[code]` ko 상수 safety net 폴백. `nonEmpty(_:)` 헬퍼 도입으로 trim/empty 가드 통일. 기존 쿠폰 플로우(`/api/coupon`, `/api/coupon/send` 등) 비경유 — coop-integration 단일 진입점만 영향. 단위 테스트 작성은 후속 별건.
> 2026-04-23: Web dev 배포본 QA 3건 해결 — ISS-030(미로그인 입력창/등록 버튼 포커스 시 `common_toast_plz_login` 토스트 선행 후 `goToLogin` 호출로 iOS/Android와 UX 정합), ISS-033(일반 쿠폰 성공 토스트 `coupon_register_success_toast` ko/ja/en 번역 키 신규 + `case 'coupon':` 분기 dispatch 추가 — iOS와 동일 키/문구), ISS-034(스킬 이용권 카드 `{data.skillName}`만 렌더 + 접미사 덧붙이기 템플릿 키 `coop_skill_voucher_name` ko/ja/en 삭제 — "쿠폰명 이용권 이용권" 중복 해소).
> 2026-04-23: **ISS-047 등록** — iOS 스킬 이용권 카드 "스킬 보러가기" 링크 en/ja 번역 누락. `CouponItemModel.swift:324` 한글 리터럴 하드코딩 결함. 문구 확정: ko "스킬 보러가기" / en "View Skill" / ja "スキルを見る" (Android `coop_link_view_skill` 3종과 통일, ISS-027 해결 시 `>` 제거 정합). design-spec.md §S4 + client-guide.md §S4 **i18n 문구 3종 확정** 명시 추가. iOS `/dev-ios` 구현 과업 발주 — ResourceKit 키 1건 × 3언어 추가 + 리터럴 교체.
> 2026-04-23: iOS QA 피드백 3건 일괄 해결 — ISS-040(이용권 배지 클라이언트 derive: `CouponItemCell.bind()`의 tags 렌더 직전 `effectiveTags` 파생 — `isSkillVoucher && !coupon.tags.contains(voucherTag)` 조건에서 "이용권" prepend, ResourceKit `coop_label_voucher` 3종 추가), ISS-041 재조정(행 collapse → reserve: `descriptionLabel.flex.isIncludedInLayout = false` 라인 제거 → 일반 쿠폰과 동일 높이 유지 + 텍스트만 숨김, 웹 ISS-031 reserve 전략과 정합), ISS-047(`coop_link_view_skill` 3종 추가 + 한글 리터럴을 `.Coupon.View.skillLink`로 치환). 수정 파일: `Hellobot/Feature/Coupon/CouponList/Views/CouponItemModel.swift`, `Modules/Common/ResourceKit/ResourceKit/Sources/Strings/String+Coupon.swift`, `Modules/Common/ResourceKit/ResourceKit/Resources/Localizable/{ko,en,ja}.lproj/Localizable.strings`. Android `coop_label_voucher`/`coop_link_view_skill` 3언어와 키명·값 완전 통일.
> 2026-04-24: **ISS-048 등록 + design-spec §S4 렌더 분기 규칙 재정의 (/architect)**. `/dev-ios` 점검에서 design-spec.md §S4 line 103-104 "모든 분기 동일 기준(AND)" 선언이 같은 문서 하위 문장(line 114 만료일 `isUnlimited` 단독 / line 116 링크 조건 비명시)과 api-spec.md 두 필드 독립성 계약과 **자기모순**임을 포착. 원인은 ISS-040 배지 derive 문구가 네 분기 전체에 과잉 일반화된 것. 재정의 방향은 "분기 2종 구조" — (1) 상품 자체 식별 분기(배지·부가설명 = AND), (2) 개별 속성 반응 분기(만료일 = `isUnlimited` 단독, 링크 = `fixedMenuSeq` 단독). design-spec §S4 상단 블록 교체 + line 116 링크 조건 명시 추가 + 레이아웃 도식 안내 추가 + Changelog 1줄. iOS/Android 코드 수정 0건(현 구현이 api-spec.md 규약과 이미 정합), Web 회귀 확인 `/dev-web` 발주. 서버·QA 영향 없음.
> 2026-04-28: **데이터 측정 문서 분리 (/dev-data)** — `architecture.md §10/§11` 본문을 두 신규 SSOT 로 이전. (1) [data-measurement-plan.md](data-measurement-plan.md) v1.0 — KPI·Q1~Q4 결정·갭. (2) [event-spec.md](event-spec.md) v1.0 — Firebase 이벤트 3종·파라미터·검증 절차. architecture.md §10/§11 자리에는 이전 안내 + `<details>` 보존 본문 유지. planning/{success-metrics-kpi.md, performance-analysis-design.md} deprecation 노트 추가. 다른 *-spec.md 컨벤션과 통일.
> 2026-04-28: **쿠폰 등록 이벤트 스펙 결정 (/dev-data)** — Firebase 클라이언트 이벤트 3종 확정. `view_coupon_register` (화면 진입), `register_coupon_success` (9개 파라미터 — coupon_number/coupon_type/issued_type/product_code/fixed_menu_seq/heart_amount/bonus_heart_amount/latency_ms), `register_coupon_failure` (6개 — coupon_number/coupon_type/coupon_prefix/error_code/reason/latency_ms). 서버 이벤트는 발화하지 않음(DB 진실 원천). 이벤트명 컨벤션: 화면=`view_*`, 액션=동사 시작, 채널은 `coupon_type` 파라미터로 분류(이벤트명에 박지 않음). 서버 응답 DTO 보강 필요 항목: `productCode`(카카오), `heartAmount`·`bonusHeartAmount`(하트 충전권). 상세: [architecture.md §11](architecture.md#11-쿠폰-등록-이벤트-스펙-2026-04-28-결정).
> 2026-04-28: **Q4 카카오 유입자 식별 결정 (ISS-049 보강)** — 첫 카카오 쿠폰 등록일 기준 시간 단위(일/주/월) 신규 분류로 확정. `coop_kakao_first_used_date` (DATE) 컬럼을 `mart_user_daily_info` + `union_mart_user_key_actions` 양쪽에 추가. RDS `coop_marketing_coupon_usage` 일 1회 스냅샷 인입 + staging/intermediate/mart SQL 4건 신규/확장. status 무관(used+canceled 모두 포함). **4/30 구현 완료 목표**. 상세 [architecture.md §10-7](architecture.md#10-7-카카오-유입자-식별-q4-결정-2026-04-28).
> 2026-04-27: **Q1 거래액 인식 정책 결정 (ISS-049)** — 카카오 100% 할인 쿠폰 결제의 거래액 인식을 `spent_cash_amount` 인젝션 방식으로 확정. 서버가 카카오 쿠폰 사용 시 `coop_marketing_product.current_price ?? price` 를 `pay_for_contents` 의 `spent_cash_amount` 파라미터에 인젝션 → `revenue_krw>0` 으로 `pay_under_750` 격리 회피 + 모든 다운스트림 매출 공식 자동 합산 (대시보드 변경 0건). 데이터 측: 카탈로그 [ISS-017](../../common-data-airflow/docs/hellobot-data/catalog/issues.md) 등록 + BQ description 4건 + 인라인 코멘트 + 카탈로그 markdown(`mart_use_skill_se.md`/`event-catalog.md`/`metric-dictionary.md`) 갱신 완료. 서버 측: `coop_marketing_product.current_price` 신설 마이그레이션 + 카카오 쿠폰 식별 + 인젝션 구현 대기. Q2 하트 충전권 매출 인식 검증 완료(`expiredAt=NULL`로 유료 하트 적립). Q3 정산 데이터 인입 불필요 결정. 상세는 [architecture.md §10 데이터 분석 설계](architecture.md#10-데이터-분석-설계).
> 2026-04-28: **서버 ISS-049 Q1 인젝션 구현 (/dev-server)** — `coop_marketing_product` `current_price` 컬럼 마이그레이션 추가 + Entity·AdminJS 옵션 노출. `CoopMarketingService.findUsageByIssuedCouponSeq` 헬퍼 추가. `FixedMenuService.purchaseFixedMenu` 의 `sendPayForContentsEvent` 호출 직전 `resolveCoopSpentCash` 헬퍼로 카카오 발급 쿠폰 사용 시 `coop_marketing_product.current_price ?? price` 를 `{ amount, currency: "KRW" }` 로 인젝션. 일반 쿠폰/쿠폰 미사용 결제는 `undefined` 유지. tsc 통과. BQ 검증은 배포 후 실시.

> 2026-04-28: **ISS-050 에러 코드 컨벤션 정합화 (/dev-server)** — 신규 코드 `CM_001~CM_010` → `CM001~CM010` (언더바 제거, 기존 `CO`/`CP` 시리즈와 동일 형식), `CO_APP_UPDATE_REQUIRED` 항목 삭제하고 의미 중복 코드 `CO012(UPDATE_APP)` 재사용으로 통합. enum 이름 의미 일치 항목은 `CP` 접미사와 통일(`CM_INVALID_COUPON`→`CM_COUPON_NOT_FOUND` 외 2건). ko 메시지 어미 "~이에요" → "~습니다" 정합화 + 의미 일치(`CM001`/`CM002`/`CM003`)는 ko/ja/en 모두 `CP001`/`CP003`/`CP002` 문구 그대로 채택. 서버: `code.ts` enum, `locales/{ko,ja,en}.ts`, `coupon.ts` controller, `coop-marketing.ts`, `coupon-register.ts`, Entity/마이그레이션 코멘트 일괄 갱신. tsc 통과. 운영 배포 전 단계로 하위 호환성 무시 — Firebase 이벤트 발화 미시작 상태도 확인. 클라이언트(iOS) 매퍼 동기 + 문서 일괄 갱신은 본 변경에 포함, Android/Web/데이터는 영향 없음 또는 enum 표기 갱신만 필요.

> 2026-04-28: **ISS-050 후속 — iOS 매퍼 동기 + QA xlsx v13 (/dev-ios + /qa, 병렬 진행)**. iOS — `CouponRegisterErrorMapper.swift`의 codeMessages 사전 키 11건 + ko 폴백 메시지 일괄 갱신, ISS-026/039 판정 로직 무변경, 다른 iOS 코드 영향 0건. QA — `qa-test-cases-v13.xlsx` 신규 생성: 시트 전체 코드 표기 `CM_xxx`/`CO_APP_UPDATE_REQUIRED` → `CMxxx`/`CO012` 정합, 기대 메시지 ko 어미 "~습니다" + CM001/002/003 ja/en CP 시리즈 문구 통일, 회귀 TC 추가. v12 감사 추적 보존. 서버·iOS 동시 배포 후 Step B 회귀 검증 대기.

> 2026-04-24: **Web ISS-048 회귀 점검 + 해결** — 4개 요소 중 3 Pass, (B-2) "스킬 보러가기 링크 `fixedMenuSeq` 단독 조건" Fail(링크가 AND 분기된 `CoopSkillVoucherItem`에서만 렌더, `fixedMenuSeq != null && !isUnlimited` 쿠폰 미지원). 권장안 적용 — `CouponItem.tsx`에 optional `onSkillLinkClick?` prop 추가 + 내부 `fixedMenuSeq != null && onSkillLinkClick` 단독 조건 분기(동일 inline SVG + `coop_skill_voucher_link` 재사용), `app/coupon/page.tsx`가 `handleSkillVoucherClick` 주입, 하단 영역 좌(만료정보)+우(링크) `space-between` 재구성, `types/coupon.ts:20` 주석 갱신. iOS/Android(4/4 Pass, 무수정)와 함께 3개 클라이언트 모두 재정의된 2종 분기 구조 정합. 현 운영 환경 해당 쿠폰 타입 없어 QA 재현 불가 — 스펙 정합성 선제 보강.

> 2026-04-29: **응답 DTO 보강 결정 — Firebase 이벤트 파라미터 의존성 (/dev-data, D1~D5 픽스)**. Android 에이전트 발견 이슈(`product_code`/`bonus_heart_amount` 응답 미제공) 해소를 위해 `POST /api/coupon/register` 성공 응답에 신규 필드 2개만 추가하기로 확정 — ① `couponType` (string, 항상; `kakao`/`hellobot`/`giftiel`, EVT-2 의 `coupon_type` 1차 소스, D1=a), ② `productCode` (string\|null, issuedType=heart/skill 시; 카카오 한정, D1=a). 초기 안에 있던 `heartAmount`+`bonusHeartAmount` 분리는 폐기 — `heartQuantity` 단일 필드 유지(D2=a). 사유: `heartAmount`/`bonusHeartAmount` 는 본 도메인에 없는 신규 명칭이었음(기존 `pay_for_contents.spent_*_heart_coin` 컨벤션을 응답 DTO 로 잘못 일반화). Q2 결정상 카카오 하트 충전권은 paid 100% 적립이며 bonus 분리 도입의 실익 없음. 에러 응답에는 `couponType` 추가하지 않음 (D3=a) — EVT-3 는 클라이언트 prefix 룩업 유지. 결과 효과: 서버 작업 4필드→2필드, 이벤트 파라미터 `heart_amount`/`bonus_heart_amount` → `heart_quantity` 단일로 단순화. 동반 갱신: event-spec.md §3.2/§3.3/§5/§11 + api-spec.md §Response/§필드 설명/Changelog + tasks.md §서버/§iOS/§데이터-§이벤트스펙. 후속: /dev-server 응답 DTO 2필드 추가 → /dev-android 신규 발화 구현 + /dev-ios·/dev-web 보강 반영 (`heart_amount` 키 → `heart_quantity` 변경, 추론 로직 제거 → 응답값 사용).
> 2026-04-29: **iOS Firebase 이벤트 3종 발화 구현 (/dev-ios)** — event-spec.md §3 SSOT 기준. 신규 파일 `Hellobot/Legacy/Analytics/Firebase/HellobotAnalytics + Coupon.swift` (`Coupon` 네임스페이스 + `Event` enum + `RegisterSuccessPayload`/`RegisterFailurePayload` 구조체 + `fire(_:)` 진입점). `CouponListViewController` 수정 — `viewDidLoad` 끝에 `fAnalytics.fire(.viewCouponRegister)` (EVT-1), `registerCoupon(code:)` `.do(onSubscribe:)`에 `requestStartedAt = Date()` 캡처, `Self.elapsedMilliseconds(since:)` static 헬퍼, onNext/onError 핸들러에서 `fireRegisterSuccessEvent` / `fireRegisterFailureEvent` 호출 (EVT-2/EVT-3). `coupon_type` 추론 — 성공: heart/skill ⇒ kakao, coupon ⇒ hellobot / 실패: prefix `90/91` ⇒ kakao, 그 외 nil. `error_code` 분류 — 서버 에러 `code` 우선, offline ⇒ `NETWORK_ERROR`, 기타 ⇒ `UNKNOWN`. 응답 DTO 미보강 필드(`productCode`, `bonusHeartAmount`)는 nil 송신(파라미터 omit). 잔여: DebugView 실기기 발화 검증, 서버 응답 DTO 보강 후 nil 송신 부분 응답값으로 교체.

> 2026-04-29: **iOS D1~D5 스펙 픽스 반영 (/dev-ios)** — event-spec.md §3.2 v1.1 + api-spec.md `data.couponType`/`productCode` 신규에 맞춰 4건 재조정. ① `CouponRegisterResponse` 에 `couponType: String?` + `productCode: String?` 옵셔널 디코딩 추가(서버 보강 PR 머지 전 무해). ② EVT-2 `coupon_type` 클라이언트 추론(`heart/skill ⇒ kakao, coupon ⇒ hellobot`) 제거 → 응답 `data.couponType` 직접 매핑(D1=a). `couponType(forSuccess:)` 헬퍼 삭제. ③ `RegisterSuccessPayload.heartAmount` → `heartQuantity` 리네임 + 발화 키 `heart_amount` → `heart_quantity`(D2=a). ④ `RegisterSuccessPayload.bonusHeartAmount` 필드/발화 키 제거(D2=a, paid 100% 결정). EVT-3 는 변경 없음(prefix 룩업 + `couponType(forPrefix:)` 유지, D3=a). 수정 파일 3건: `CouponRegisterResponse.swift`, `HellobotAnalytics + Coupon.swift`, `CouponListViewController.swift`. 발화 미시작 단계라 다운스트림 영향 없음 — 서버 보강 배포 시점부터 `coupon_type`/`product_code` 자동 채워짐.

> 2026-04-29: **서버 D1~D5 응답 DTO 보강 구현 (/dev-server)** — `POST /api/coupon/register` 성공 응답 `data` 에 신규 필드 2개 추가. ① `couponType` (string, 항상; `kakao`/`hellobot`/`giftiel`) — 입력 코드 prefix → `coupon_prefix_rule` 시드 매칭으로 산출 (등록 처리 흐름에서 이미 도출되는 값을 응답 직렬화 시 노출). ② `productCode` (string\|null, issuedType=heart/skill 한정) — 카카오 채널일 때 `coop_marketing_product.product_code` 매핑 (`CoopMarketingService.findUsageByIssuedCouponSeq` 재사용), 비카카오 채널은 NULL. `heartQuantity` 그대로 유지(D2=a). 에러 응답 변경 없음(D3=a). DTO/Service 레이어 갱신 + 회귀·통합 테스트. 클라이언트 3사 unblock. 커밋 `f59b0b65`.
> 2026-04-29: **Web Firebase 이벤트 3종 발화 구현 + D1~D5 응답 DTO 매핑 (/dev-web)** — event-spec.md §3 v1.1 + api-spec.md `data.couponType`/`productCode` 신규 SSOT 기준. `lib/features/coupon/couponGoogleAnalytics.ts` 신규 (채널 분기: `webview.isWebView()` → `webview.logEvent` 또는 `window.gtag('event', ...)`, `skillsGoogleAnalytics.ts` 패턴 동일). `app/coupon/page.tsx` 마운트 1회 `view_coupon_register` 발화. `app/coupon/components/couponCodeRegister.tsx` 등록 버튼 탭 → 응답까지 `performance.now()` 로 `latency_ms` 측정 후 success/failure(network·empty body·unsupported issuedType 포함 4분기) 발화. EVT-2 `coupon_type` 은 응답 `data.couponType` 직접 사용 (D1=a, prefix 룩업 제거), `product_code` 응답 `data.productCode` 매핑(null 시 omit), 파라미터 키 `heart_amount` → `heart_quantity` (D2=a, `bonus_heart_amount` 폐기). EVT-3 는 prefix 룩업 유지 (D3=a). 타입 정의 `CouponRegisterResponse` discriminated union 에 `couponType`/`productCode` 추가. **hellobot-webview(Angular)는 발화 추가 없음** — `coupon.component.ts`가 `ngOnInit`에서 즉시 `${origin}/coupon`(Next.js) 리다이렉트만 수행하는 stub. 커밋 `8e18e1bd`. 잔여: 화이트리스트 등록 후 D+1 BQ 도달 검증.
> 2026-04-29: **Android Firebase 이벤트 3종 발화 구현 + D1~D5 스펙 픽스 (/dev-android)** — event-spec.md v1.1 §3 SSOT + 2026-04-29 결정 직접 반영. 신규 파일 `app/src/main/java/com/thingsflow/hellobot/util/analytics/event/firebase/FirebaseCoupon.kt` — `FirebaseCoupon` sealed class + `ViewCouponRegister` data object + `RegisterCouponSuccess`/`RegisterCouponFailure` data class + companion object 의 prefix 룩업 유틸(`PREFIX_TO_COUPON_TYPE = {90,91 → kakao}` 시드 동기). null 파라미터는 Bundle 키 자체 omit(Firebase 결측 분포 분석). `CouponListActivity.onCreate` 에서 `savedInstanceState == null` 가드 후 EVT-1 발화(회전 등 재생성 중복 방지, iOS `viewDidLoad` 1회 의미와 동일). `CouponListViewModel.register` 진입 직후 `requestStartedAt = System.currentTimeMillis()` 캡처, 성공/에러 핸들러에서 `latencyMs = now - requestStartedAt` 산출 후 `fireRegisterSuccessEvent`/`fireRegisterFailureEvent` 호출. `CouponRegisterResponse` 에 `couponType: String? = null` + `productCode: String? = null` 필드 추가(D1=a, 서버 보강 PR 머지 전엔 null 도착 → omit, 머지 후 자동 채워져 발화). 에러 분류 신설 — `parseServerError(error)` + `parseErrorBody(body)`: `HttpException` → 응답 `error.code`(CM001~CM010·CO012·CP*) 우선, `IOException`(SocketTimeout/UnknownHost/Connect 등 모두 하위) → `NETWORK_ERROR`, 기타 → `UNKNOWN`. 기존 `extractServerMessage` 는 `parseServerError(...).message` 로 흡수(폴백 호환 유지) — `_coopEvent.emit(CoopEvent.ShowError(parsed.message))` 인터페이스 불변, 토스트 UX 영향 없음. EVT-3 `coupon_type` 은 D3=a(에러 응답 미보강) 따라 클라이언트 prefix 룩업, 매칭 실패 시 omit. `coupon_prefix` 는 입력값 앞 2자리 항상 발화. 검증: `:app:ktlintCheck` BUILD SUCCESSFUL, `:app:assembleDevDebug` BUILD SUCCESSFUL. 잔여: DebugView 실기기 발화 검증 + 화이트리스트 등록은 후속(/dev-data). iOS/Web 의 04-29 기 구현은 D1~D5 결정 직전 패턴 → 04-29 후속 픽스로 정합화 완료, Android 는 D1~D5 직접 반영으로 차이 없음.

## 파트별 현황

| 파트 | 상태 | 브랜치 | 워크트리 | 비고 |
|------|------|--------|---------|------|
| 기획 | 진행중 | - | - | 스킬 이용권 라인업 선정, 최종 상품 구성 확정 잔여 |
| 디자인 | 완료 (Phase 1 반영 완료) | - | - | 2026-04-19: S2 확인 팝업 제거 반영 (1단계 전환) |
| 서버 | **운영 배포 완료 (2026-04-30)** + 핫픽스 2건 적용 + 후속 안정화 진행 | feat/coupnc-integration (머지됨) | worktrees/hellobot-server/, worktrees/hellobot-server-hotfix/ (정리 가능), worktrees/hellobot-server-hotfix-url/ (정리 가능) | Phase 1 구현 완료 (04-19): `POST /api/coupon/register`, `CouponPrefixRule` 엔티티+시드, 진입 가드, `registerOneShot`(Redlock), AdminJS, `/api/coop/*` @deprecated. ISS-021 무제한 유효기간 (04-21). ISS-022 해결 (04-21). **ISS-049 Q1 인젝션 구현 (04-28)** — `current_price` 마이그레이션·Entity·AdminJS, `findUsageByIssuedCouponSeq` 헬퍼, `purchaseFixedMenu` 의 `resolveCoopSpentCash` 통한 `spent_cash_amount` 인젝션. **ISS-050 에러 코드 컨벤션 정합화 (04-28)** — `CM_001~CM_010` → `CM001~CM010`, `CO_APP_UPDATE_REQUIRED` → `CO012(UPDATE_APP)` 통합, enum 이름 CP 접미사 통일, ko 어미 정합. **D1~D5 응답 DTO 보강 (04-29, 커밋 `f59b0b65`)** — `POST /api/coupon/register` 성공 응답 `data` 에 `couponType`(항상), `productCode`(heart/skill, 카카오 한정) 신규 추가. `heartQuantity` 유지(D2=a). 에러 응답 변경 없음(D3=a). **운영 배포 완료 (2026-04-30, master `00d6f57b`)**. **핫픽스 #1 ISS-053 (04-30, PR #2401, commit `f2f12a84`)** — coop-marketing catch 3곳 `saveApiLog`(varchar(4) 위반 22001 마스킹) 제거 + `winston.error` 마스킹 로그. **핫픽스 #2 ISS-054 (04-30, PR #2403, commit `8372dadc`)** — 운영 URL 스킴 `http://...:443` → `https://.../AuthUse`. 사용자 curl 검증 운영 통신 정상. 잔여: 메트릭 수집(ISS-009 #11), API 테스트 추가(#11), Admin 정산 통계 custom page(B1, #5), Phase 2 `/api/coop/*` 제거(#14), deploy-dev #2403 cherry-pick(낮음, 동작 영향 없음). ISS-052 (L1+L3 동시 실패 자동 복구) — CS 유의미 발생 시 구현 |
| iOS | Phase 1 완료 + ISS-050 매퍼 동기 + Firebase 이벤트 3종 발화 + D1~D5 스펙 픽스 반영 완료 (서버·iOS 동시 배포 대기) | feat/coop-integration | worktrees/hellobot_iOS/ | Phase 1 전환 완료 (04-19). ISS-018 해결 (04-19). ISS-019/021/022/023/026/027 해결 (04-21). ISS-030/033/035/038/041 해결 (04-22). ISS-042 해결 (04-22). **ISS-039 해결 (04-23)** — `CouponRegisterErrorMapper` 판정 완화(containsHangul 제거, non-empty 서버값 우선). **ISS-040/041 재조정/047 해결 (04-23)** — 이용권 배지 클라이언트 derive + 박스 높이 reserve 전환 + 스킬 보러가기 en/ja 번역. **ISS-050 매퍼 동기 (04-28)** — `CouponRegisterErrorMapper` codeMessages 키 11건(`CM_001~CM_010`/`CO_APP_UPDATE_REQUIRED` → `CM001~CM010`/`CO012`) + ko 폴백 메시지 어미 정합화. 판정 로직 무변경. 다른 iOS 코드 영향 0건 확인. **Firebase 이벤트 (04-29)** — `view_coupon_register`/`register_coupon_success`/`register_coupon_failure` 3종 발화 (event-spec.md §3). `HellobotAnalytics + Coupon.swift` 신규 + `CouponListViewController` 진입/응답 핸들러 wiring + latency_ms 측정. **D1~D5 스펙 픽스 (04-29)** — `CouponRegisterResponse` 에 `couponType`/`productCode` 옵셔널 디코딩 추가 + EVT-2 `coupon_type` 추론 제거(응답 직접 매핑) + 키 `heart_amount`→`heart_quantity` 리네임 + `bonus_heart_amount` 제거. EVT-3 prefix 룩업 유지. 잔여: ISS-036(실기기 재현 확인), 카카오 딥링크 진입 처리, DebugView 발화 검증 |
| Android | Phase 1 완료 + Firebase 이벤트 3종 발화 + D1~D5 스펙 픽스 반영 완료 (서버 배포 대기) | feat/coop-integration | worktrees/hellobot_android/ | Phase 1 전환 완료 (04-19). ISS-016/019/020/022/024/028 해결 (04-21). ISS-027 해결 (04-22). **Firebase 이벤트 + D1~D5 (04-29)** — `view_coupon_register`/`register_coupon_success`/`register_coupon_failure` 3종 발화 (event-spec.md v1.1). 신규 `FirebaseCoupon.kt` (sealed class + prefix 룩업 유틸 `90`/`91` → kakao) + `CouponListActivity.onCreate` `savedInstanceState == null` 가드 진입 발화 + `CouponListViewModel.register` `requestStartedAt` 캡처 후 success/failure wiring + `latencyMs` 측정. `CouponRegisterResponse` 에 `couponType`/`productCode` 옵셔널 추가(서버 보강 PR 머지 시 자동 채워져 발화). 에러 분류 `parseServerError` 신설 — `HttpException` → `error.code`(CM001~CM010·CO012·CP*), `IOException` → `NETWORK_ERROR`, 기타 → `UNKNOWN`. EVT-3 `coupon_type` 은 D3=a 따라 클라이언트 prefix 룩업. null 파라미터 omit. `:app:ktlintCheck` + `:app:assembleDevDebug` BUILD SUCCESSFUL. 잔여: 카카오 딥링크 진입 처리, DebugView 실기기 발화 검증, 화이트리스트 등록(/dev-data) |
| 웹 | Phase 1 완료 + Firebase 이벤트 3종 발화 + D1~D5 응답 DTO 매핑 완료 (서버 배포 대기) | feat/coop-integration | worktrees/hellobot-web/ | Phase 1 전환 완료 (04-19~20). ISS-017 해결 (04-19). ISS-016/022/025 해결 (04-21). ISS-031/032/035/036/037/042 해결 (04-22). **ISS-030/033/034 해결 (04-23)**. **ISS-048 해결 (04-24)** — `CouponItem`에 `fixedMenuSeq` 단독 조건 스킬 링크 분기 추가로 재정의된 2종 분기 구조 정합. **Firebase 이벤트 + D1~D5 (04-29, 커밋 `8e18e1bd`)** — `view_coupon_register`/`register_coupon_success`/`register_coupon_failure` 3종 발화 (event-spec.md v1.1). `couponGoogleAnalytics.ts` 신규 (채널 분기: webview vs gtag). EVT-2 `coupon_type` 응답 `data.couponType` 직접 사용 (prefix 룩업 제거, D1=a), `product_code` 매핑(D1=a), 키 `heart_amount`→`heart_quantity`(D2=a, bonus 폐기). EVT-3 prefix 룩업 유지(D3=a). `CouponRegisterResponse` 타입에 `couponType`/`productCode` 추가. hellobot-webview(Angular)는 stub 으로 영향 없음. 잔여: DebugView 발화 검증, 화이트리스트 등록 후 D+1 BQ 도달 검증 |
| 스튜디오 | 해당없음 | - | - | |
| 데이터 | Q1~Q4 결정 완료 (구현 진행) | Feat/coop-integration | worktrees/common-data-airflow/ | 2026-04-22 설계 초안 작성. **Q1 (2026-04-27)** `spent_cash_amount` 인젝션 결정 (architecture.md §10-1~6, ISS-017). **Q2 (2026-04-27)** 하트 충전권 = 유료 하트 검증 완료. **Q3 (2026-04-27)** 정산 데이터 인입 불필요. **Q4 (2026-04-28)** 카카오 유입자 식별 — 등록일 기준 일/주/월 신규 분류, `coop_kakao_first_used_date` 컬럼 추가 (architecture.md §10-7). 카탈로그 6건 갱신 완료. **ISS-050 (2026-04-28)**: `register_coupon_failure.error_code` enum 표기만 갱신(미발화 단계로 다운스트림 영향 없음, event-spec.md 동기 완료). 잔여 (4/30 데드라인): RDS 스냅샷 인입 + staging/intermediate/mart SQL 4건 + DAG 체인 검증 |
| QA | v13 생성 완료 (ISS-050 정합), Step B 검증 대기 | - | - | 145건 (폐기 63건 제거 후). 플랫폼별 재편성 (Web 44, iOS 20, AOS 20, Admin·Server 47 + Skip 2). xlsx v5 생성 (5 시트: Web, iOS, Android, Admin·Server, 요약). TC ID 체계 변경 (TC-W-*, TC-I-*, TC-A-*, P1-*, TC-M*/N*/A*-DEP). 이전 버전 v4 감사 추적 보존. v11 생성 (04-21 보강 7건, 총 152건). **v12 (ISS-048 반영, 156건)** — Flow V에 TC-V-BR01~04 4건 추가(2종 분기 매트릭스), 기존 UL/S/D TC에 판정 기준 명시. **v13 생성 (04-28 ISS-050 반영)** — 코드 표기 `CM_xxx`/`CO_APP_UPDATE_REQUIRED` → `CMxxx`/`CO012` 일괄, ko 어미 "~습니다" 정합 + CM001/002/003 ja/en CP 문구 통일, 회귀 TC 추가. v12 감사 추적 보존. Step B 검증 대기 (서버·iOS 배포 후) |

## 잔여 과업 요약

| # | 과업 | 파트 | 블로커 | 비고 |
|---|------|------|--------|------|
| 1 | ~~Phase 1 서버 구현 (register + 가드 + PrefixRule)~~ | 서버 | — | ✅ 완료 (04-19) |
| 2 | ~~Phase 1 클라이언트 구현 (분기 제거 + 단일 API + S2 삭제)~~ | iOS/Android/Web | — | ✅ 완료 (04-19~20) |
| 3 | Phase 1 QA 재검증 | QA | #2 완료 | 구버전 회귀 포함. TC v10 기준 플랫폼 매트릭스 검증 진행 |
| 4 | 카카오 딥링크 진입 처리 | iOS, Android | 딥링크 스킴 미확정 | 기존 COUPON 딥링크 재사용 검토 |
| 5 | Admin 정산 통계 custom page | 서버 | 없음 | 운영 배포 전 필요 |
| 6 | ~~웹뷰 환경 검증~~ | 웹 | — | ✅ 해당 없음 종결 (04-21). 앱은 네이티브 `CouponListViewController`/`CouponListActivity`, 웹 `/coupon`은 스킬스토어 브라우저 전용 (architecture.md §6) |
| 7 | ~~일본어 번역 검수~~ | 웹 | — | ✅ 완료 |
| 8 | ~~ISS-019 스킬 이용권 카드 "스킬 보러가기 >" 링크~~ | Android | — | ✅ 해결 (04-21) |
| 9 | ~~ISS-020 스킬 이용권 등록 후 스킬 팝업 즉시 노출 제거 → 토스트 + 리스트 업데이트~~ | Android | — | ✅ 해결 (04-21) |
| 10 | ~~ISS-016 에러 토스트 지속시간 2.5초~~ | Android | — | ✅ 해결 (04-21, SafeToast) |
| 11 | 서버 ISS-009 메트릭/로그 수집 + API 테스트 추가 | 서버 | 없음 | 가드 발동 빈도 관측 |
| 12 | 스킬 이용권 라인업 선정 | 기획 | 없음 | |
| 13 | 최종 상품 구성 확정 | 기획 | #12 완료 후 | |
| 14 | Phase 2 `/api/coop/*` 엔드포인트 제거 | 서버 | Phase 1 전파트 배포 완료 후 | 별도 릴리스 |
| 15 | ~~ISS-025 웹 스킬 카드 재진입 시 하단 유실 수정~~ | 웹 | — | ✅ 해결 (04-21). Notion DLT-HLB-1002 |
| 16 | ~~ISS-026 iOS 쿠폰 에러 메시지 표시 오류 (영문/빈 토스트)~~ | iOS | — | ✅ 해결 (04-21). Notion DLT-HLB-1016 |
| 17 | ~~ISS-027 스킬 보러가기 화살표 이미지 리소스 교체~~ | iOS/Android | — | ✅ iOS 해결 (04-21), Android 해결 (04-22). Notion DLT-HLB-1020 |
| 18 | ~~ISS-028 Android S3 모달 여백·이미지 크기 정합성~~ | Android | — | ✅ 해결 (04-21). Notion DLT-HLB-1022 |
| 19 | ~~ISS-029 AdminJS 메뉴명 한글화 ("쿠폰 분류 규칙 설정")~~ | 서버 | — | ✅ 해결 (04-22, `src/admin/locale.ts` labels 추가). Notion DLT-HLB-1026 |
| 20 | ~~D1~D5 응답 DTO 보강 (서버) — `couponType`/`productCode` 신규~~ | 서버 | — | ✅ 완료 (04-29, 커밋 `f59b0b65`) |
| 21 | ~~Firebase 이벤트 3종 발화 (iOS) + D1~D5 매핑~~ | iOS | — | ✅ 완료 (04-29, 커밋 `99000cbbb`) |
| 22 | ~~Firebase 이벤트 3종 발화 (Android) + D1~D5 매핑~~ | Android | — | ✅ 완료 (04-29, 커밋 `9117b647e`) |
| 23 | ~~Firebase 이벤트 3종 발화 (Web) + D1~D5 매핑~~ | 웹 | — | ✅ 완료 (04-29, 커밋 `8e18e1bd`) |
| 24 | **Q4 데이터 파이프라인** — RDS 스냅샷 + staging/intermediate/mart SQL 4건 + DAG 체인 검증 | 데이터 | 없음 | **4/30 데드라인 D-1**. data-measurement-plan.md §6 + tasks.md §데이터 §Q4 |
| 25 | Firebase 화이트리스트 등록 — `view_coupon_register`/`register_coupon_success`/`register_coupon_failure` 3건 | 데이터 | #21·22·23 발화 시작 후 D+1 BQ 도달 확인 | `hlb_staging.staging_key_events_fb_events_list` (+ `events_list`) BQ 수동 INSERT |
| 26 | event-catalog.md SSOT 갱신 — §유스케이스 색인·§4-1 신규 카테고리 "쿠폰 등록" 3행·§Changelog 4행 누적 (Q1 시멘틱 + 신규 3종) | 데이터 | #25 와 동시 PR | common-data-airflow 카탈로그 sync 의무 |
| 27 | DebugView 실기기 발화 검증 (3종 모두) | iOS, Android, Web | 클라이언트 배포 후 | `-FIRDebugEnabled` + Firebase 콘솔 |
| 28 | QA 통합 테스트 — Q1 인젝션·EVT-1/2/3 발화·Q4 카카오 유입자 분류·KPI 쿼리 | QA | #20·24 완료 후 | qa-test-cases 통합 검증 |
| 29 | Q3 외부 발급 데이터 옵션 결정 (A/B/C) | 분석/PM | 비즈니스 협의 | data-measurement-plan §7.1 권장 B (월 1회 CSV). 출시 후 첫 R4 까지 결정해도 무방 |
| 30 | ~~핫픽스 #1 ISS-053 (varchar(4) 위반)~~ | 서버 | — | ✅ 해결 (04-30, PR #2401 commit `f2f12a84`, deploy-dev cherry-pick `c5245d05`) |
| 31 | ~~핫픽스 #2 ISS-054 (운영 URL 스킴)~~ | 서버 | — | ✅ 해결 (04-30, PR #2403 commit `8372dadc`). 운영 사용자 curl 검증으로 통신 정상 확인 |
| 32 | deploy-dev `8372dadc` cherry-pick (코드 정합성, 동작 영향 없음) | 서버 | 없음 | 우선순위 낮음 — dev URL 변경 없어 행동 차이 없음 |
| 33 | ISS-052 (L1+L3 동시 실패 시 (나) 자동 복구) | 서버 | CS 유의미 발생 시 | `coop_marketing_recovery_pending` 테이블 + 5분 주기 배치 + L0 재조회 자동 보상. 현재는 winston 로그 기반 CS 수동 처리 |
| 34 | 운영 핫픽스 워크트리 정리 — `worktrees/hellobot-server-hotfix/`, `worktrees/hellobot-server-hotfix-url/` | 서버 | PR 머지 완료 후 안정 기간 경과 | `git worktree remove` |

## 블로커

- 운영 배포 + 핫픽스 2건 적용 완료. 운영 흐름 동작 검증은 사용자 보고 + 사용자 curl 로 통신 정상 확인됨
- **#24 (Q4 파이프라인)** 4/30 데드라인 — 데이터 파트 구현 진행 (서버 의존 없음)
- 서버 D1~D5 보강 PR 머지·배포 완료 (#20) → 클라이언트 발화 작업은 모두 완료 (#21~23) → 화이트리스트 등록(#25)·DebugView 검증(#27)·QA(#28) 진행 가능 단계 진입

## Phase 1 배포 순서 (엄수)

상세: [architecture.md §9](./architecture.md), [tasks.md 의존 관계](./tasks.md)

```
서버 프로덕션 배포
    ↓ (헬스체크 + 스모크 테스트 통과)
웹 배포 + iOS/Android 앱스토어 제출
    ↓ (신버전 릴리스 순차 진행)
구버전 잔존율 모니터링
    ↓ (4주 경과 + 호출률 < 0.1%)
Phase 2: /api/coop/* 엔드포인트 제거
```

**위험**: 클라이언트 선행 배포 시 신버전 클라이언트가 존재하지 않는 엔드포인트 호출 → 장애. 서버 선행 필수.
**롤백**: 서버 롤백 전 신버전 클라이언트를 먼저 되돌려야 안전.

## 확정 사항

| 항목 | 내용 |
|------|------|
| 상용 인증키 (Auth_Key) | 개발/상용 동일 |
| 쿠폰번호 프리픽스 | 90, 91 (DB `coupon_prefix_rule` 시드 데이터) |
| 쿠폰 형식 판별 | 서버 단일 진입점 (`POST /api/coupon/register`) — 확정 2026-04-19 |
| 쿠폰 등록 단계 | 1단계 원샷 — 확정 2026-04-19 |
| 구버전 앱 대응 | 서버 `/api/coupon` 가드 → `CO_APP_UPDATE_REQUIRED` 토스트 — 확정 2026-04-19 |
| 앱 업데이트 메시지 | "앱 업데이트가 필요한 쿠폰이에요." — 확정 2026-04-19 |
| 기획-디자인 정합성 | 검토 완료 (04-18). S2 팝업 제거 반영 (04-19) |
