# 디자인 스펙 — 카카오 선물하기 상품권

## Figma 원본

- **확정 디자인**: https://www.figma.com/design/hedYMUTlEPft16RaXdHAn0/-26.04--%EC%B9%B4%EC%B9%B4%EC%98%A4%EC%84%A0%EB%AC%BC%ED%95%98%EA%B8%B0-%EC%BF%A0%ED%8F%B0-%EB%B0%9C%EA%B8%89-%EB%B0%8F-%EC%82%AC%EC%9A%A9?node-id=1-7235&m=dev
- **디자인 페이지**: `1:7235` (🎨 디자인)
- **섹션**: App (`1:9442`), Web (`7:2607`), 이용권 쿠폰 설명 (`10:10900`), Flow (`9:5632`)

> ⚠️ **폐기된 프레임 주의** (2026-04-19 이후):
> - `23:4064` (S2-A 하트 확인 팝업), `23:4095` (S2-B 스킬 확인 팝업) — 1단계 원샷 플로우 전환으로 **구현 대상 아님** (ISS-011 해결). Figma 파일에는 잔존하므로 참고 금지. 제거 또는 `@Deprecated` 표기 권장.
> - `23:3813`, `23:5028` (S6 로그인 안내 팝업) — 기획 변경으로 폐기 (2026-04-16).

---

## 공통 스타일

### 디자인 토큰 — 컬러

| 토큰 | 색상 | 용도 |
|------|------|------|
| PRIMARY YELLOW 400 | `#FFE967` | 확인/충전/받기 버튼, 로그인 안내 배너 |
| GRAY 900 | `#242526` | 기본 텍스트, 버튼 텍스트 |
| GRAY 600 | `#7E8185` | 보조 텍스트, 만료일 텍스트, 이용권 태그 |
| GRAY 500 | `#A4A7AD` | placeholder, 빈 상태 텍스트 |
| GRAY 400 | `#C6C8CC` | 비활성 버튼 배경 |
| GRAY 200 | `#EDEDEE` | 취소 버튼 배경, 카드 테두리, 이용권 태그 테두리 |
| GRAY 100 | `#F5F5F5` | 인풋 배경 |
| WHITE | `#FFFFFF` | 팝업/카드 배경 |
| SUB RED 400 | `#FF5D7A` | 캡션 (하트 충전 쿠폰/스킬 이용 쿠폰), 만료 임박 |
| SUB RED 600 | `#F23658` | 만료 임박 "N일 남음" |
| SUB PURPLE | `#BE7AFE` | "스킬 보러가기 >" 링크 |
| Overlay | `rgba(36,37,38,0.7)` | 팝업 배경 오버레이 |
| Toast BG | `rgba(36,37,38,0.8)` | 토스트 배경 |

### 디자인 토큰 — 타이포그래피

| 토큰 | 폰트 | 크기 | 굵기 | 행간 | 자간 | 용도 |
|------|------|------|------|------|------|------|
| Heading 2 | Pretendard | 22px | Bold (700) | 28px | -0.1px | 팝업 제목 |
| Body 3 (Bold) | Pretendard | 16px | SemiBold (600) | 24px | -0.1px | 버튼 텍스트 |
| Label 1 | Pretendard | 14px | Regular (400) | 20px | -0.1px | 팝업 본문 설명 |
| Caption 1 | Pretendard | 12px | Regular (400) | 16px | -0.1px | 캡션 (쿠폰 유형) |
| Body 3 (App) | Apple SD Gothic Neo | 16px | Regular (400) | 24px | -0.6px | 앱 본문 |
| Body 1 (App) | Apple SD Gothic Neo | 13px | Regular (400) | 19px | -0.3px | 앱 안내 배너 |

### 공통 컴포넌트 — 팝업

| 속성 | 값 |
|------|-----|
| 너비 | 288px |
| border-radius | 20px |
| padding | 24px |
| 그림자 | `0px 8px 24px rgba(0,0,0,0.24)` |
| 오버레이 | `rgba(36,37,38,0.7)` |
| 내부 구조 | textSet (gap 12px) + buttonSet (gap 8px), 전체 gap 20px |

### 공통 컴포넌트 — 버튼 (Button/Sub)

| 속성 | 확인 버튼 | 취소 버튼 |
|------|---------|---------|
| 배경 | `#FFE967` | `#EDEDEE` |
| 텍스트 색상 | `#242526` | `#242526` |
| 높이 | 48px | 48px |
| border-radius | 26px |
| padding | 12px 24px |
| min-width | 100px |
| 폰트 | Pretendard SemiBold 16px |
| 레이아웃 | flex: 1 (2버튼 시 균등 분할, gap 8px) |

### 공통 컴포넌트 — 등록 버튼

| 상태 | 배경 | 텍스트 |
|------|------|--------|
| 활성 | `#FFE967` | `#242526` "등록" |
| 비활성 | `#C6C8CC` | `#EDEDEE` "등록" |

---

## 화면별 스펙

### ~~S2-A: 하트 충전 확인 팝업~~ (2026-04-19 제거)

**ISS-011 해결 과정에서 1단계 원샷 플로우로 전환되어 제거됨.** 쿠폰 등록 시 확인 팝업 없이 서버가 즉시 발급 처리합니다. Figma 디자인(`23:4064`)은 참고용으로만 유지. 클라이언트 구현 시 이 팝업을 만들지 않습니다.

### ~~S2-B: 스킬 교환 확인 팝업~~ (2026-04-19 제거)

**ISS-011 해결 과정에서 1단계 원샷 플로우로 전환되어 제거됨.** 쿠폰 등록 시 확인 팝업 없이 서버가 즉시 발급 처리합니다. Figma 디자인(`23:4095`)은 참고용으로만 유지. 클라이언트 구현 시 이 팝업을 만들지 않습니다.

### S3: 하트 충전 완료 팝업

| 요소 | 스펙 |
|------|------|
| 일러스트 | 캐릭터+하트 이미지 (`img_heart_complete.png`), 높이 약 140px |
| 캡션 | 이벤트/상품명 — Caption 1, `#FF5D7A` |
| 제목 | "하트가 {N}개 충전되었어요!" — Heading 2, `#242526` |
| 본문 | "하트는 \<프로필\>탭에서 확인 가능해요" — Label 1, `#7E8185` |
| 버튼 | "확인" 노란 단일 버튼 (취소 버튼 없음) |

### S4: 스킬 이용권 카드

기존 쿠폰 카드 형식을 따름.

**스킬 이용권 카드 렌더 분기 규칙 (클라이언트 공통)**

api-spec.md는 `GET /api/coupon` 응답의 두 필드를 **의미적으로 독립**하도록 정의한다 — `fixedMenuSeq`는 "단일 스킬에 고정된 쿠폰의 스킬 식별자"(api-spec.md:249), `isUnlimited`는 "유효기간 무제한"(api-spec.md:251). 현재 스킬 이용권은 우연히 두 속성을 모두 가지지만, 두 필드는 서로 직교(orthogonal)한 개념이다. 따라서 스킬 이용권 카드의 분기도 **두 종류로 나누어 적용**한다:

1. **"스킬 이용권 상품 자체를 식별"하는 분기** — 판별 기준 `isUnlimited === true && fixedMenuSeq != null` (AND)
   - **배지 "이용권" 노출** (ISS-040)
   - **부가 설명 행 숨김** (ISS-041/046, invisible reserve)
   - 사유: "이 쿠폰이 스킬 이용권 상품 그 자체다"라는 **의미론적 판단**. 두 속성이 모두 있어야 스킬 이용권으로 분류.

2. **"개별 필드 속성에 반응"하는 분기** — 각 필드 단독 조건 (api-spec.md 적용 규칙 §247-258 준수)
   - **만료일 / 만료임박 숨김** (ISS-021/022): `isUnlimited === true` 단독 — 향후 "무제한 일반 쿠폰"이 도입되어도 만료일이 자연스럽게 숨겨지도록 (api-spec.md:251 "카드 UI에서 만료일/만료임박 행 미표시 권장")
   - **"스킬 보러가기" 링크 노출** (ISS-019/024): `fixedMenuSeq != null` 단독 — 향후 "스킬 지정 + 유한 만료" 쿠폰이 도입되어도 스킬 상세 숏컷이 유지되도록 (api-spec.md:257 "존재 시 쿠폰 카드 탭 → SkillDetail(fixedMenuSeq) 네비게이션")

> ⚠️ 과거(~2026-04-23) "모든 분기 동일 기준(AND)" 서술은 ISS-040 배지 derive 문구의 과잉 일반화였으며, api-spec.md의 필드 독립성 계약과 모순이라 폐기. 2026-04-24 `/architect`가 api-spec.md 정합성 방향으로 재정의. 코드(iOS/Android)는 이미 api-spec 규약을 따르고 있어 코드 수정 영향 없음. Web은 회귀 확인 대상.

| 요소 | 스펙 |
|------|------|
| 카드 배경 | white, border `#EDEDEE` |
| 라벨 | "이용권" 태그 — 11px Bold, `#7E8185`, border `#EDEDEE`, 흰 배경 |
| 할인율 | "100%" — 18px Bold, `#FF5D7A` |
| 쿠폰명 | 스킬명 — 16px Bold, `#242526` |
| 부가 설명 (`description`) | "N하트 이상 결제 시 사용 가능" — 12px Regular, `#7E8185`. **조건부 처리 (스킬 이용권, ISS-041/046)**: 스킬 이용권은 최소 결제 조건이 의미 없으므로 **내용은 숨기되 공간은 유지 (invisible reserve)**. 일반 쿠폰 카드와 동일 수직 리듬/높이 유지. 접근성 트리에서는 읽히지 않아야 함 |
| 챗봇명 | 미노출 (opacity: 0) |
| 만료일 | "YYYY.MM.DD HH:mm까지" — 12px Regular, `#7E8185`. **조건부 표시**: 서버 응답 `isUnlimited === true`이면 **미표시** (스킬 이용권은 무제한, ISS-021/022) |
| 만료 임박 | "N일 남음" — 12px Regular, `#F23658` + 구분선 (1px, `#EDEDEE`). **조건부 표시**: `isUnlimited === true`이면 미표시 |
| 링크 | "스킬 보러가기 >" — 12px Bold, `#BE7AFE`. **노출 조건 (ISS-019/024, ISS-048)**: `fixedMenuSeq != null` **단독 조건** (`isUnlimited`와 무관). 선택 화면(iOS CouponSelect 등 선택 모드)에서는 노출 억제. **i18n (ISS-047)**: ko "스킬 보러가기" / en "View Skill" / ja "スキルを見る" — `>`는 **텍스트에 포함하지 않고** chevron 이미지(`icon_arrow_right_16` 또는 SF Symbol `chevron.right`, violet400 틴트)로 분리 렌더 (ISS-027 해결 정합) |
| 동작 | 카드 탭 → 스킬 상세 페이지 이동 |

**레이아웃** (Figma node `10:12563`, `10:12592`, `10:12594`, `10:12622` 공통):

```
┌─────────────────────────────────────────┐
│ 100%                      [이용권][앱전용]  │  상단: 좌=할인율, 우=태그 (앱전용은 조건부)
│ {{스킬명}} 이용권                          │
│ (부가 설명 자리 — invisible reserve)        │  ← 스킬 이용권은 공간만 유지, 문구 미노출
│ ┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈ │  점선 구분선 1px `#EDEDEE`
│ [N일 남음 │] 유효기간        스킬 보러가기 > │  하단: 좌=기한, 우=링크
└─────────────────────────────────────────┘
```

- **상단 영역**: 좌측에 할인율(`100%`), 우측에 태그들(`이용권`, 필요시 `앱전용` 추가) — `space-between` 정렬
- **부가 설명 행 (invisible reserve)**: 스킬 이용권 카드에서는 문구 자체(`"0하트 이상 결제 시 사용 가능"`)가 무의미하므로 **시각적으로 숨기되 레이아웃 공간은 유지**. 일반 쿠폰과 동일한 카드 높이/수직 리듬을 확보하여 쿠폰함 리스트에서 두 카드 타입이 섞여도 시각적 어긋남이 없도록 함. 스크린리더(TalkBack/VoiceOver)는 해당 행을 **읽지 않아야 함**.
- **하단 영역**: 좌측에 (만료 임박 "N일 남음" + 구분선 `│`)? + 유효기간, 우측에 "스킬 보러가기 >" 링크 — `space-between` 정렬
- "스킬 보러가기 >" 링크는 **반드시 하단 우측 정렬** (좌측 정렬 금지)
- **무제한 쿠폰 (`isUnlimited === true`)**: 하단 좌측 영역을 비워 렌더링하고, 점선 구분선도 숨김(또는 링크만 표시되는 단일 행으로 대체). sentinel 날짜(`2099-12-31T23:59:59.000Z`) 직접 비교 금지 — 반드시 `isUnlimited` 플래그로 분기
- **각 요소의 표시 조건**: 위 "스킬 이용권 카드 렌더 분기 규칙" 참조. 요소별로 `isSkillVoucher`(AND)·`isUnlimited` 단독·`fixedMenuSeq` 단독 조건이 **의도적으로 다름**

**플랫폼별 구현 레시피 (부가 설명 invisible reserve)**:
- **Web**: Tailwind `invisible` 클래스 (display 유지, visibility hidden). 또는 `visibility: hidden`. — ISS-031 구현 참조.
- **Android (Compose)**: `Modifier.alpha(0f)` + `Modifier.clearAndSetSemantics { }` 체이닝. `alpha` 단독은 TalkBack이 여전히 읽으므로 semantics 차단 필수. — ISS-046 구현 참조.
- **iOS (UIKit)**: `descriptionLabel.isHidden = true` + **FlexLayout `isIncludedInLayout = true` 유지**(기본값, FlexLayout에서 `isHidden`만으로는 공간이 collapse되지 않음). `isIncludedInLayout = false` 조합(collapse)은 금지 — 카드 높이가 축소되어 일반 쿠폰과 불일치. ISS-041 재조정(2026-04-23)으로 이미 적용 완료. 접근성은 `isHidden = true` 상태에서 VoiceOver가 자동 제외함.

> ⚠️ collapse(`display: none` / `isHidden = true` + `flex.isIncludedInLayout = false`) 방식 **금지**. 스펙 위반. 플랫폼 관계없이 reserve로 통일 (2026-04-24 /architect 결정, 3 플랫폼 구현 완료 상태).

### S6: 미로그인 시 로그인 화면 직접 이동 (기획 변경: 2026-04-16)

~~기존: 로그인 안내 팝업 (Figma node `23:3813`, `23:5028`)~~ → **변경: 로그인 화면으로 직접 이동**

Figma에 팝업 디자인은 잔존하나, 기획 확정에 따라 **팝업 없이 로그인 화면으로 직접 이동**합니다.

#### 미로그인 쿠폰 화면 — 로그인 안내 배너

미로그인 상태에서 쿠폰 화면 상단에 노란색 배너를 표시합니다. (Figma: App `9:8750`, Web `9:8755`)

| 요소 | 스펙 |
|------|------|
| 배너 배경 | `#FFE967` (PRIMARY YELLOW 400) |
| 텍스트 | "로그인하면 더 많은 쿠폰을 받을 수 있어요 **로그인하기 >**" |
| X 닫기 버튼 | 배너 우측, 탭 시 배너 숨김 |
| 배너 위치 | 헤더 바로 아래, 입력란 위 |

#### 미로그인 시 동작

| 트리거 | 동작 |
|--------|------|
| 입력란 포커스 | 로그인 화면으로 즉시 이동 |
| 등록 버튼 탭 | 로그인 화면으로 즉시 이동 |
| 배너 "로그인하기 >" 탭 | 로그인 화면으로 즉시 이동 |

#### 로그인 화면 (Figma: App `207:6717`, Web `207:6706`)

| 요소 | App | Web |
|------|-----|-----|
| 일러스트 | hellobot 캐릭터 (고양이+물고기) | hellobot 캐릭터 그룹 |
| 로그인 버튼 1 | "카카오톡으로 로그인" (노란 버튼) | "카카오톡으로 로그인" (노란 버튼) |
| 로그인 버튼 2 | "Apple로 로그인" (검정 버튼) | — |
| 기타 | "다른 방법으로 로그인 ∨" + "로그인 해주세요" 툴팁 | "다른 방법으로 로그인 ∨" |
| 로그인 후 | 쿠폰 페이지로 복귀 (fallbackUrl/딥링크) | 쿠폰 페이지로 리다이렉트 (fallbackUrl=/coupon) |

### 에러 토스트

| 요소 | 스펙 |
|------|------|
| 배경 | `rgba(36,37,38,0.8)`, radius 8px |
| 텍스트 | 14px Regular, white, tracking -0.3px |
| 패딩 | px 20px, pt 13px, pb 11px |
| 위치 | 하단 중앙 |
| 자동 사라짐 | 2.5초 |

### 쿠폰 입력 영역

| 요소 | 스펙 |
|------|------|
| 인풋 배경 | `#F5F5F5`, radius 20px, h 40px, px 16px |
| placeholder | "쿠폰 코드를 입력해주세요" — 16px Regular, `#A4A7AD` |
| X 클리어 버튼 | 입력 중일 때 표시 |

---

## 에셋 목록

| 파일명 | 용도 | 크기 | 포맷 | Figma Node |
|--------|------|------|------|-----------|
| ~~`icon_heart_24.svg`~~ | ~~S2-A 하트 확인 팝업 아이콘~~ | ~~24x24~~ | ~~SVG~~ | ~~`23:4091`~~ | **S2-A 제거로 불필요** (2026-04-19) |
| ~~`icon_coupon_24.svg`~~ | ~~S2-B 스킬 확인 팝업 아이콘~~ | ~~24x24~~ | ~~SVG~~ | ~~`23:4100`~~ | **S2-B 제거로 불필요** (2026-04-19) |
| ~~`icon_warning_24.svg`~~ | ~~S6 로그인 안내 팝업 경고 아이콘~~ | ~~24x24~~ | ~~SVG~~ | ~~`23:3938`~~ | **S6 팝업 폐기로 불필요** |
| `img_heart_complete.png` | S3 하트 완료 팝업 일러스트 | 1184x576 | PNG | `9:5569` |

> 앱(iOS/Android)에서 동일 에셋이 필요합니다. Figma에서 각 플랫폼 규격에 맞게 export하세요.

---

## 파트별 가이드

### 웹 (hellobot-web) — 반영 완료

- **스택**: Next.js 14 / React 18 / Tailwind CSS
- **에셋 경로**: `public/images/coop/`
- **팝업 컴포넌트 경로**: `app/coupon/components/coop*.tsx`
- 주요 Tailwind 매핑:
  - 팝업: `w-[288px] rounded-[20px] p-[24px] shadow-[0px_8px_24px_rgba(0,0,0,0.24)]`
  - 확인 버튼: `bg-[#FFE967] h-[48px] rounded-[26px]`
  - 취소 버튼: `bg-[#EDEDEE] h-[48px] rounded-[26px]`
  - 오버레이: `bg-[rgba(36,37,38,0.7)]`
- 상세 리뷰: [designs/design-review.md](./designs/design-review.md) — 전항목 체크 완료

### iOS (hellobot_iOS) — 미착수

- **폰트**: 앱 기본 폰트는 Apple SD Gothic Neo, 팝업 내부는 Pretendard
- **에셋**: Figma에서 @1x/@2x/@3x 규격으로 export 필요
- **팝업 구현**: UIKit 기반 커스텀 모달 또는 기존 팝업 컴포넌트 활용
- **토스트**: 기존 앱 토스트 컴포넌트가 있으면 재사용, 없으면 스타일 맞춰 구현
- **이용권 카드**: 기존 쿠폰 카드 UI에 "이용권" 라벨 + "스킬 보러가기" 링크 추가
  - 하단 영역은 **좌우 분할** (`HStack` + `Spacer`): 좌=유효기간(+ 선택적 "N일 남음"), 우=**"스킬 보러가기 >" 링크 (우측 정렬 필수)**
- **미로그인 처리**: 로그인 안내 배너 (노란색, X 닫기) + 입력 시도 시 로그인 화면 직접 이동 (팝업 없음)

### Android (hellobot_android) — 개발중

- **폰트**: Pretendard (팝업), 시스템 폰트 (앱 기본)
- **에셋**: Figma에서 mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi 규격으로 export 필요
- **팝업 구현**: DialogFragment 또는 Compose Dialog
- **dp 변환**: 디자인 기준 375px 폭 → dp로 비율 변환 (288px → ~288dp 그대로 사용 가능, 모바일 기준)
- **컬러**: colors.xml에 디자인 토큰 컬러 추가 또는 기존 매핑 확인
- **이용권 카드**: RecyclerView 내 기존 쿠폰 카드 ViewHolder에 이용권 타입 분기 추가
- **미로그인 처리**: 로그인 안내 배너 (노란색, X 닫기) + 입력 시도 시 로그인 화면 직접 이동 (팝업 없음)

---

## 디자인 리뷰 체크리스트

### 웹 — 반영 완료 (S2 제거 작업 Phase 1 예정)

- [x] ~~S2-A: 팝업 크기, 아이콘, 캡션, 제목, 버튼~~ → 1단계 전환으로 제거 예정 (Phase 1)
- [x] ~~S2-B: 팝업 크기, 아이콘, 캡션, 제목, 버튼~~ → 1단계 전환으로 제거 예정 (Phase 1)
- [x] S3: 일러스트, 제목, 단일 버튼
- [x] S4: 카드 형식, 라벨, 할인율, 만료일, 링크
- [x] ~~S6: 경고 아이콘, 제목, dim 닫기 차단, 1회 노출 제한~~ → 기획 변경으로 폐기
- [x] S6: 미로그인 시 로그인 화면 직접 리다이렉트 (팝업 → goToLogin)
- [x] 에러 토스트: 스타일, 자동 사라짐
- [x] 인풋 placeholder 변경

### iOS — Phase 1 재착수 (기존 S2 구현물 제거)

- [ ] ~~S2-A: 팝업 크기(288px), 아이콘(heart 24px), 캡션 색상(#FF5D7A), 버튼 스타일~~ → 1단계 전환으로 구현 대상 제외
- [ ] ~~S2-B: 팝업 크기, 아이콘(coupon 24px), 버튼 "받기"~~ → 1단계 전환으로 구현 대상 제외
- [ ] S3: 일러스트 이미지, 단일 "확인" 버튼
- [ ] S4: 이용권 태그, 100% 할인율, 만료일 포맷, "스킬 보러가기" 링크 (**하단 우측 정렬**)
- [ ] S6: 미로그인 시 로그인 안내 배너 (노란색, X 닫기) + 입력 시도 시 로그인 화면 직접 이동
- [ ] 에러 토스트: 스타일, 2.5초 자동 사라짐
- [ ] 인풋 placeholder: "쿠폰 코드를 입력해주세요"

### Android — Phase 1 재착수 (기존 S2 구현물 제거)

- [ ] ~~S2-A: 팝업 크기(288dp), 아이콘(heart 24dp), 캡션 색상, 버튼 스타일~~ → 1단계 전환으로 구현 대상 제외
- [ ] ~~S2-B: 팝업 크기, 아이콘(coupon 24dp), 버튼 "받기"~~ → 1단계 전환으로 구현 대상 제외
- [ ] S3: 일러스트 이미지, 단일 "확인" 버튼
- [ ] S4: 이용권 태그, 100% 할인율, 만료일 포맷, "스킬 보러가기" 링크 (**하단 우측 정렬**)
- [ ] S6: 미로그인 시 로그인 안내 배너 (노란색, X 닫기) + 입력 시도 시 로그인 화면 직접 이동
- [ ] 에러 토스트: 스타일, 2.5초 자동 사라짐
- [ ] 인풋 placeholder: "쿠폰 코드를 입력해주세요"

---

## Changelog

| 날짜 | 변경자 | 변경 내용 | 확인 |
|------|--------|----------|------|
| 2026-04-24 | /architect | **S4 "스킬 이용권 카드 렌더 분기 규칙" 재정의 (ISS-048)**: 기존 "모든 분기 동일 기준(AND)" 선언을 폐기. api-spec.md의 두 필드 독립성 계약(`fixedMenuSeq`=스킬 식별자, `isUnlimited`=유효기간 무제한)과 정합하도록 **2종 분기 구조로 재정의** — (1) "상품 자체 식별" 분기(배지 ISS-040 / 부가설명 ISS-041·046): `isUnlimited && fixedMenuSeq != null` **AND**. (2) "개별 속성 반응" 분기(만료일 ISS-021·022: `isUnlimited` 단독 / 링크 ISS-019·024: `fixedMenuSeq != null` 단독). 링크 행(line 116)에 노출 조건 명시 추가. 레이아웃 도식 하단 "요소별 조건 의도적으로 다름" 문구 추가. iOS/Android는 이미 api-spec 규약을 따르고 있어 **코드 수정 영향 없음**. Web은 `/dev-web` 회귀 확인 필요. | iOS/Android 무수정 확정, Web 회귀 확인 대기 |
| 2026-04-24 | /architect | **S4 스킬 이용권 카드 판별 기준 + 부가 설명 행 처리 규칙 명문화 (ISS-041/046 정합)**: (1) §S4 상단에 "스킬 이용권 카드 판별 기준: `isUnlimited === true && fixedMenuSeq != null` (배지/링크/만료일/부가설명 분기 공통 단일 소스)" 선언. (2) 요소 표에 "부가 설명(description)" 행 신규 추가 — 스킬 이용권은 **invisible reserve** (문구 숨김 + 공간 유지 + 스크린리더 차단). (3) ascii 레이아웃 도식에 부가 설명 행 추가. (4) 플랫폼별 구현 레시피 추가 — Web `invisible`, Android `Modifier.alpha(0f) + clearAndSetSemantics`, iOS `isHidden = true` (FlexLayout `isIncludedInLayout` **유지**). (5) collapse 방식 금지 명시. 3 플랫폼 모두 reserve로 이미 구현됨(Web ISS-031, Android ISS-046, iOS ISS-041 재조정 2026-04-23) — 본 변경은 사후 명문화. **※ 상단 "판별 기준: 단일 소스(AND)" 선언은 2026-04-24 재정의(ISS-048)로 폐기 — 본 Changelog 상위 항목 참조.** | 전파트 구현 정합 확인 완료 |
| 2026-04-19 | /architect | **ISS-011 해결 반영**: S2-A(하트 확인)/S2-B(스킬 확인) 팝업 섹션을 취소선 처리 + 제거 사유 명시 (1단계 원샷 플로우 전환). 에셋 `icon_heart_24.svg`, `icon_coupon_24.svg` 불필요 처리. 리뷰 체크리스트의 S2 항목 제외. | 전파트 구현 재진행 예정 |
| 2026-04-16 | /design | 최초 작성 (기존 designs/ 스펙 + Figma 추출 통합) | /dev-web (기반영) |
| 2026-04-16 | /design | S6 기획 변경 반영: 로그인 안내 팝업 → 로그인 화면 직접 이동. 로그인 안내 배너 스펙 추가, 로그인 화면 컴포넌트(App `207:6717`, Web `207:6706`) 추가, `icon_warning_24.svg` 불필요 처리, iOS/Android 리뷰 체크리스트 S6 항목 변경 | |
| 2026-04-16 | /design | 에셋 노드ID 수정: `icon_heart_24.svg` `23:4085`→`23:4091`, `icon_coupon_24.svg` `23:4233`→`23:4100` (기존 노드 삭제됨, 실제 인스턴스 노드로 정정) | |
| 2026-04-21 | /design | S4 이용권 카드 레이아웃 명시 보강 (ISS-023): 하단 영역 좌우 분할 — 좌=유효기간(+선택적 "N일 남음"), 우=**"스킬 보러가기 >" 링크 (우측 정렬 필수)**. iOS/Android 리뷰 체크리스트에 "하단 우측 정렬" 명시 추가 | /dev-ios (확인 필요) |
| 2026-04-21 | /analyze | S4 카드 만료일/만료 임박 행에 **조건부 표시** 명시 (ISS-021/022): `isUnlimited === true`이면 미표시. 레이아웃 설명에 무제한 쿠폰 케이스(하단 좌측 비움, 점선 구분선 숨김) 추가. sentinel 직접 비교 금지 재명시 | 전파트 구현 반영 완료 |
| 2026-04-23 | /analyze | S4 "스킬 보러가기 >" 링크 i18n 문구 3종 확정 명시 (ISS-047): ko "스킬 보러가기" / en "View Skill" / ja "スキルを見る". `>`는 텍스트 포함 금지 — chevron 이미지(ISS-027 해결 정합)로 분리 렌더 원칙 추가. Android는 기존 `coop_link_view_skill`(ISS-027 시 `>` 제거됨)로 이미 충족, iOS는 ISS-047 과업으로 대응 필요 | iOS (/dev-ios) 구현 대기 |
