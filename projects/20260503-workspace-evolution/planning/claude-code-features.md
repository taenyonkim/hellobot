# Claude Code 기능 카탈로그 — 워크스페이스 적용 매핑 (1차 시드)

> 본 문서는 1차 시드. 누락된 기능, 워크스페이스에 의미 있는 적용 후보를 추가로 발굴하는 것이 Phase 1-A 의 후속 과업.
>
> 표기 규칙:
> - **현재 사용도**: 미사용 / 부분사용 / 적극사용
> - **도입 우선순위**: P1 (즉시 효과) / P2 (검증 후 도입) / P3 (장기 검토)

---

## 1. 슬래시 커맨드 (Slash Commands)

| 항목 | 값 |
|------|----|
| 정체 | `.claude/commands/*.md` 의 프롬프트 — 호출 시 본문이 시스템 메시지로 인입 |
| 현재 사용도 | **적극사용** (11개 운영) |
| 강점 | 역할별 컨텍스트 격리 + 일관된 절차 강제 |
| 한계 | 한 컨텍스트 / 한 모델 — 호출 사이 학습이 누적되지 않음. 본문 길이 늘면 토큰 부담 |
| 도입 우선순위 | (이미 사용 중) — 본 프로젝트에서는 **본문 다이어트 + 도메인 참조 절차 추가** |

### 적용 매핑

- 현재 모든 도메인 규칙을 커맨드 본문에 박아 두면 본문이 비대 → 별도 도메인 문서로 외출하고 커맨드는 "어떤 문서를 어떤 순서로 로드할지" 만 지시
- 예: `/dev-server` 본문에 "쿠폰 도메인 작업 시 `hellobot-server/docs/domain/coupon.md` 를 먼저 로드" 한 줄

---

## 2. CLAUDE.md (계층적 컨텍스트)

| 항목 | 값 |
|------|----|
| 정체 | 워크스페이스/리포/디렉토리/사용자 글로벌 4단 계층, 자동 컨텍스트 인입 |
| 현재 사용도 | 부분사용 — 워크스페이스 CLAUDE.md만 풍부, 리포별은 편차 큼 |
| 강점 | 항상 로드 → 가장 신뢰도 높은 컨텍스트 채널 |
| 한계 | 너무 비대해지면 토큰 비용 + 핵심 규칙 묻힘 |
| 도입 우선순위 | **P1** — 도메인 지식 진입점 / 리포 CLAUDE.md 보강 |

### 적용 매핑

- 각 리포 CLAUDE.md 에 "도메인 작업 시 `docs/domain/index.md` 부터 읽어라" 진입 가이드 추가
- 워크스페이스 CLAUDE.md 의 누적된 규칙을 카테고리별로 분리 — 예: 컨텍스트 효율성, 이슈 관리는 별도 docs/로 외출

---

## 3. Hooks (자동화 트리거)

| 항목 | 값 |
|------|----|
| 정체 | 도구 호출 / 사용자 메시지 / Stop / 세션 시작 등 시점에 셸 명령 실행 |
| 현재 사용도 | **미사용** |
| 강점 | "에이전트가 잊지 않게" 강제 — 결정론적 자동화 |
| 한계 | 셸 환경에 의존 / 디버깅이 어려움 / 실패 시 작업 차단 가능 |
| 도입 우선순위 | **P1** — status 동기화 / Changelog 자동화 / 도메인 문서 변경 알림 |

### 적용 매핑 후보

| 시점 | 자동화 후보 |
|------|------------|
| `PostToolUse` (Edit/Write 후) | 변경 파일이 계약 문서면 Changelog 미기재 경고 |
| `PostToolUse` (`tasks.md` 체크박스 변경 후) | 프로젝트 status.md 의 파트별 현황 자동 반영 검증 |
| `Stop` (에이전트 작업 종료) | 도메인 의사결정이 발생했는데 ADR 저장이 없으면 알림 |
| `SessionStart` | 워크스페이스 정합성 빠른 점검 (오래된 status, 미해결 이슈 카운트) |

> **주의**: hooks는 "잊지 않기 위한 안전망" 으로만 쓰고, 의사결정 자동화는 피한다 (false positive 가 작업 차단으로 이어짐).

---

## 4. Subagents (Agent Tool)

| 항목 | 값 |
|------|----|
| 정체 | 별도 컨텍스트 / 도구 한정 / 결과만 메인으로 반환하는 격리 에이전트 |
| 현재 사용도 | **미사용** (메인 컨텍스트에서 직접 작업) |
| 강점 | 메인 컨텍스트 보호 (대량 탐색을 분리) / 병렬화 |
| 한계 | 결과 손실 가능 (요약본만 반환) / 호출 비용 |
| 도입 우선순위 | **P2** — 광범위 탐색이 필요한 경우 |

### 적용 매핑 후보

| 시나리오 | subagent 활용 |
|---------|--------------|
| `/dev-server` 가 도메인 코드 베이스 광범위 탐색 | `Explore` 서브에이전트로 위임 |
| 신규 프로젝트 분석 시 영향 범위 조사 | `general-purpose` 또는 자체 정의 에이전트 |
| 크로스 리포 영향 분석 | 리포별 subagent 병렬 호출 |

> 현재 `/dev-*` 들이 메인 컨텍스트에서 직접 grep/glob 하여 컨텍스트가 부풀음 — Explore 위임으로 핵심 정보만 회수

---

## 5. Skills

| 항목 | 값 |
|------|----|
| 정체 | 재사용 가능한 절차/지식 묶음. 명시 호출 또는 트리거 단어로 활성화 |
| 현재 사용도 | **미사용** (명시 호출 가능한 외부 스킬은 시스템 차원에서 노출되어 있으나 활용 안 함) |
| 강점 | 도메인 지식의 모듈화 / 트리거 기반 활성화 |
| 한계 | description 작성 품질에 활용도가 좌우됨 / 메타 학습 곡선 |
| 도입 우선순위 | **P2** — 반복 패턴 (레시피) 의 영속 저장소 후보 |

### 적용 매핑 후보

| 카테고리 | 스킬 후보 |
|---------|----------|
| 반복 작업 레시피 | "TypeORM 마이그레이션 추가", "BQ 마트 추가", "AdminJS 리소스 추가" |
| 도메인 절차 | "쿠폰 발급/취소 플로우 작업 시 점검 항목" |
| 검증 절차 | "이벤트 발화 5단계 검증" (이미 event-spec.md 에 정형화됨 → 스킬로 승격 후보) |

> **주의**: 스킬과 도메인 문서(`docs/domain/`)의 경계가 모호해질 수 있다. 원칙: 스킬 = 호출 가능한 절차, 도메인 문서 = 읽고 이해하는 자료.

---

## 6. MCP Servers

| 항목 | 값 |
|------|----|
| 정체 | 외부 시스템 / 도구를 도구 호출 인터페이스로 노출 |
| 현재 사용도 | 부분사용 — Figma / Notion / Slack / Chrome 일부, 등록만 되어 있는 경우 다수 |
| 강점 | 외부 컨텍스트 통합 / 표준화 |
| 한계 | 인증 / 안정성 / 도구 schema 비대화 |
| 도입 우선순위 | **P2** — 이미 등록된 것의 활용 패턴 정착 |

### 적용 매핑 후보

| 외부 자원 | MCP 활용 |
|----------|---------|
| Figma | `/design` 에이전트가 직접 디자인 스펙 추출 (이미 적용 중) |
| Notion | `/analyze` 가 외부 1pager / 결정 노트 인입, `/workspace` 가 종료 시 결과 발행 |
| Slack | `/dev-data` 의 데이터 이슈 포스팅 자동화 (P3) |
| Chrome | QA 검증 자동화 (P3) — `/qa` 의 테스트 케이스 실행 |

---

## 7. 메모리 시스템 (`~/.claude/projects/.../memory/`)

| 항목 | 값 |
|------|----|
| 정체 | 사용자/피드백/프로젝트/참조 4가지 카테고리의 영속 메모. MEMORY.md 가 인덱스 |
| 현재 사용도 | **부분사용** — 5건만 등록, 운영 중 추가 갱신 거의 없음 |
| 강점 | 세션 간 영속 / 항상 로드되는 인덱스 |
| 한계 | 200줄 이후 truncate / 인덱스 비대화 시 신호 약화 |
| 도입 우선순위 | **P1** — 도메인 지식의 인덱스로 격상 후보 |

### 현재 메모리 상태

- `taenyon 식별자` (user)
- `HelloBot 프로젝트 개요` (project)
- `컨텍스트 효율성 규칙` (feedback)
- `에이전트 워크플로우` (project)
- `Excel 수정 방식` (feedback)
- `데이터 인프라 진입 문서` (reference)

### 적용 매핑

| 용도 | 메모리 활용 |
|------|------------|
| 도메인 진입점 인덱스 | `reference_*` 로 각 리포의 `docs/domain/index.md` 가리키기 |
| 누적 의사결정 단축 메모 | `feedback_*` — 반복 발생한 결정의 1줄 요약 |
| 사용자 작업 패턴 | `user_*` — 누적된 선호 (예: 한국어 응답, 단답 선호 등 — 이미 일부 반영됨) |

> **원칙**: 메모리는 인덱스, 본문은 docs/. 메모리에 도메인 지식 본문을 직접 넣으면 컨텍스트 비대화.

---

## 8. Settings (`.claude/settings.json` / `settings.local.json`)

| 항목 | 값 |
|------|----|
| 정체 | 권한 / 환경변수 / hooks / status line 정의 |
| 현재 사용도 | 부분사용 — 권한 허용 누적, 그 외는 미설정 |
| 도입 우선순위 | **P1** — hooks 도입 시 정착지 |

### 현재 상태 / 개선 후보

- `settings.local.json` 의 권한 허용이 누적되어 가독성 저하 — 카테고리별 정리 필요
- hooks 섹션 미사용
- `statusLine` 정의로 "현재 컨텍스트의 활성 프로젝트 / 워크트리" 표시 가능 (P3)

---

## 9. Plan Mode / Worktree Mode

| 항목 | 값 |
|------|----|
| 정체 | Plan = 코드 수정 차단된 사고 모드 / Worktree = 격리된 작업 디렉토리 |
| 현재 사용도 | Worktree 적극사용. Plan Mode 부분사용 |
| 도입 우선순위 | (이미 사용 중) — Plan Mode 를 `/architect` 단계의 디폴트로 명시 |

---

## 10. Scheduled Tasks / Cron / Loop

| 항목 | 값 |
|------|----|
| 정체 | 주기 실행 / 자율 루프 / 일정 후 재시작 |
| 현재 사용도 | **미사용** |
| 도입 우선순위 | **P3** — 정량적 효과 검증 후 |

### 적용 매핑 후보

| 시나리오 | 활용 |
|---------|------|
| 매일 오전 워크스페이스 점검 (status 동기화 / 미해결 이슈 카운트) | scheduled task |
| 장기 프로젝트의 자동 진척 점검 | loop |
| PR 머지 알림 후 status §종료 정보 자동 갱신 보조 | cron + hook 조합 |

---

## 11. TodoWrite / Plan / 컨텍스트 압축

| 항목 | 값 |
|------|----|
| 정체 | 세션 내 과업 추적 / 압축 |
| 현재 사용도 | TodoWrite 부분사용 (대형 작업 시) |
| 도입 우선순위 | (이미 사용 중) — 명시 가이드 추가만 |

---

## 13. 외부 도구 통합 — cmux 워커 spawn 패턴

| 항목 | 값 |
|------|----|
| 정체 | cmux 의 `new-workspace` 로 별도 Claude 세션을 spawn → 코디네이터(현재 세션)가 워커에 자기완결 브리핑을 주입 → 코디네이터는 사용자와 다음 대화를 즉시 재개. 워커는 독립 OS 프로세스로 병렬 실행. |
| 현재 사용도 | **미사용** — 검증만 완료 ([TODO-002](../../../todos/TODO-002-cmux-spawn-verification.md), 2026-05-13) |
| 강점 | 진짜 병렬 (코디네이터가 사용자와 다른 컨텍스트로 대화하는 동안 워커 독립 실행) / cmux notification·페인 링으로 워커 상태 푸시 (코디네이터 폴링 불요) / 메인 세션 컨텍스트 보호 |
| 한계 | 워커는 코디네이터 컨텍스트를 모름 — 자기완결 브리핑 필수 / 사용자는 한 명이라 워커가 입력 요청하면 사용자가 워크스페이스 이동 / **cmux 가 macOS 전용** — 팀 표준화 시 제약 |
| 도입 우선순위 | **P1** — 즉시 효과 + 추가 작성 거의 없음. 코디네이터 운영 규칙에 패턴 명문화만 필요. |

### Subagent (§4) 와의 차이

| 측면 | Subagent (Agent Tool) | cmux 워커 spawn |
|------|----------------------|----------------|
| 격리 단위 | 메인 세션 내 격리 컨텍스트 | OS 프로세스 단위 격리 |
| 결과 회수 | 메인이 결과 await (블로킹) | 코디네이터 즉시 복귀, 워커가 비동기로 사용자·코디네이터에 알림 |
| 사용자 상호작용 | 메인이 대신 (서브에이전트는 사용자와 대화 안 함) | 워커가 직접 사용자와 대화 가능 (사용자가 워크스페이스 전환) |
| 적합 작업 | 단발 탐색·조사 | 다중 턴 자율 작업, 사용자 협의 필요한 작업 |

> 두 메커니즘은 상호 보완. 광범위 탐색은 subagent, 다중 턴 자율 작업은 cmux 워커.

### 적용 매핑 후보

| 시나리오 | cmux spawn 활용 |
|---------|-----------------|
| 분리 커밋·코드 리뷰 같은 기계적 자기완결 작업 | 워커 spawn → 자율 실행 → 완료 알림 |
| `/dev-data` 의 장시간 BigQuery 분석·DAG 수정 | 워커 spawn → 코디네이터는 사용자와 다른 작업 진행 |
| `/qa` 의 다중 케이스 검증 수행 | 워커 spawn → 결과 보고 |
| 동시 다중 프로젝트 운영 | 활성 프로젝트당 워크스페이스 1개 |
| `/architect` + `/dev-data` 병렬 (api-spec 과 data-measurement-plan 동시 작성) | 두 워커 spawn (의존 관계 없을 때) |

### 표준 절차 (검증 완료, [TODO-002](../../../todos/TODO-002-cmux-spawn-verification.md))

```bash
# 1. 워커 spawn (Claude 세션 자동 실행)
cmux new-workspace --name "{작업명}" --cwd {path} --command "claude" --focus false
# → "OK workspace:N"

# 2. attach 대기 (~1초) — surface 의 tty 가 채워질 때까지

# 3. 자기완결 브리핑 첫 메시지 주입 (권한·금지·보고 형식·종료 조건 포함)
cmux send --workspace workspace:N "<브리핑>"
cmux send-key --workspace workspace:N Enter

# 4. 코디네이터는 즉시 사용자와 다음 대화 재개

# 5. 워커 진행 보고 싶을 때 (다른 워크스페이스 read 시 --workspace + --surface 양쪽 명시 필수)
cmux read-screen --workspace workspace:N --surface surface:M --lines 50

# 6. 워커 완료 후 정리
cmux close-workspace --workspace workspace:N
```

### 함정 (검증 시 발견)

1. **ref 모호성**: 워크스페이스가 여러 개일 때 `--surface surface:N` 단독은 모호 → `"Surface is not a terminal"` 에러. read 계열은 `--workspace + --surface` 양쪽 명시. (`send` / `send-key` 는 `--workspace` 만으로도 동작.)
2. **spawn 직후 attach 지연**: `tree` 가 surface 를 보여주긴 하지만 `tty=` 가 비어 있는 상태에서 첫 read 실패. 1초 대기 또는 `tty=` 채워질 때까지 폴링.

### 운영 규칙 정착 (후속 작업)

- `CLAUDE.md` §기본 코디네이터 §직접 처리 vs 위임 표에 "장시간 자기완결 작업 → cmux 워커 spawn" 항목 추가 후보
- 워커 브리핑 **표준 템플릿** 정착 — 권한 / 금지 / 보고 형식 / 종료 조건 / 컨텍스트 한정 (어디까지 알아야 하는지) 5요소
- 워커 브리핑은 본 세션에서 쓰는 `/workspace` 등 스킬 호출용 프롬프트와 동일 원리 — 자기완결성이 핵심

> **주의**: cmux 는 macOS 전용. 팀원이 다른 OS 로 협업할 가능성이 있으면 본 패턴은 사용자 개인 워크플로우 한정. 팀 표준화 메커니즘은 OS-agnostic 한 skill / subagent 우선.

---

## 12. 정리: 본 프로젝트의 적용 우선순위

| 우선순위 | 항목 | 효과 / 비용 |
|---------|------|------------|
| **P1** | 메모리 시스템 인덱스 격상 | 효과: 도메인 진입점 즉시 확보 / 비용: 낮음 |
| **P1** | 리포 CLAUDE.md 보강 + `docs/domain` 진입점 | 효과: 컨텍스트 진입 비용 절감 / 비용: 리포별 시드 작성 부담 |
| **P1** | hooks 도입 (status 동기화 / Changelog 미기재 경고) | 효과: "잊지 않기" / 비용: 셸 스크립트 + 디버깅 |
| **P1** | cmux 워커 spawn 패턴 정착 (§13) | 효과: 진짜 병렬 작업 / 비용: 매우 낮음 (검증 완료, 운영 규칙 명문화만) |
| **P2** | Subagent (Explore) 활용 정착 | 효과: 메인 컨텍스트 보호 / 비용: 호출 학습 |
| **P2** | 반복 작업 스킬화 | 효과: 절차 표준화 / 비용: 스킬 작성 학습 |
| **P3** | Scheduled task / Loop | 효과: 자동 점검 / 비용: 신뢰성 검증 필요 |

---

## 후속 보강 항목 (Phase 1-A 후속)

- [ ] 공식 변경 이력 기반 누락 기능 점검
- [ ] 각 후보의 도입 비용 정량화 (예상 작성 시간 / 디버깅 위험)
- [ ] 워크스페이스 외부의 hellobot 리포에서 이미 활용 중인 패턴 점검
